 package com.cric.player.util;
 
 import com.cric.player.domain.User;
 
 public class UtilValidateUser extends UtilValidate {
 	
 	public static boolean validateUser(User user) throws Exception {
 		boolean ret = false;
 		
 //		ret = isEmail(user.getUserId());
 //		ret = isAlphanumeric(user.getPassword());
 		
 		return ret;
 	}
 
 }